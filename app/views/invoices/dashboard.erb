<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
    <link rel="stylesheet" type="text/css" href="/reset.css">
    <link rel="stylesheet" type="text/css" href="/main.css">
    <title>Invoices Dashboard</title>
  </head>

  <body>
    <header>
      <h1><a href="/">Little Shop</a></h1>
      <nav>
        <ul>
          <li><a href="/merchants">Merchants</a></li>
          <li><a href="/items">Items</a></li>
          <li><a href="/invoices">Invoices</a></li>
        </ul>
      </nav>
    </header>
    <section class="header-section">
      <ul>
        <li><h2>Invoices Dashboard</h2></li>
      </ul>
    </section>
    <div class="cards">
      <ul>
        <li class="card item">
          <p class="card-header">Status Percentages</p>
          <p class="card-label">Pending: <%= Invoice.status_percentage(status: "pending") %>%</p>
          <p class="card-label">Shipped: <%= Invoice.status_percentage(status: "shipped") %>%</p>
          <p class="card-label">Returned: <%= Invoice.status_percentage(status: "returned") %>%</p>
        </li>

        <li class="card item">
          <p class="card-header">Highest/Lowest Total Charges</p>
          <p class="card-label">Invoice with Highest Total Charges:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.highest_quantity.id %>"><%= Invoice.highest_total_charges.id %></a></p>
          <p class="card-label">Invoice with Lowest Total Charges:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.lowest_quantity.id %>"><%= Invoice.lowest_total_charges.id %></a></p>
        </li>

        <li class="card item">
          <p class="card-header">Highest/Lowest Unit Price</p>
          <p class="card-label">Invoice with Highest Unit Price:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.highest_unit_price.id %>"><%= Invoice.highest_unit_price.id %></a></p>
          <p class="card-label">Invoice with Lowest Unit Price:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.lowest_unit_price.id %>"><%= Invoice.lowest_unit_price.id %></a></p>
        </li>

        <li class="card item">
          <p class="card-header">Highest/Lowest Total Quantity</p>
          <p class="card-label">Invoice with Highest Total Quantity:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.highest_quantity.id %>"><%= Invoice.highest_quantity.id %></a></p>
          <p class="card-label">Invoice with Lowest Total Quantity:</p>
          <p class="card-label"><a href="/invoices/<%= Invoice.lowest_quantity.id %>"><%= Invoice.lowest_quantity.id %></a></p>
        </li>

      </ul>
    </div>
  </body>
</html>
